{
  "catalogId": "https://banorte-tec.local/catalogs/banorte/v1/catalog.json",
  "version": "1.0.0",
  "description": "Catalogo de componentes de Tu rumbo Banorte. Fuente de verdad del protocolo: el agente solo puede pedir componentes listados aqui.",
  "components": {
    "Text": {
      "phase": "primitive",
      "description": "Texto. Usar para titulos, parrafos y notas al pie.",
      "props": {
        "text": {
          "type": "string",
          "required": true,
          "bindable": true
        },
        "variant": {
          "type": "enum",
          "values": [
            "h1",
            "h2",
            "h3",
            "body",
            "caption"
          ],
          "default": "body"
        }
      }
    },
    "CashflowChart": {
      "phase": "analizar",
      "description": "Cashflow como GRAFICA DE AREA: ingreso contra gasto mes a mes. El hueco entre las dos curvas es lo que le queda al usuario. Los datos salen de get_financial_history: los ocho meses de enero a agosto de 2026, en orden. Nunca inventes meses.",
      "props": {
        "titulo": {
          "type": "string",
          "required": true
        },
        "subtitulo": {
          "type": "string"
        },
        "serie": {
          "type": "array<{ mes: string, ingreso: number, gasto: number }>",
          "required": true,
          "bindable": true,
          "description": "mes es la etiqueta corta (\"Ene\"). ingreso y gasto son NUMEROS copiados de meses[].ingreso y meses[].gastos."
        }
      }
    },
    "ExplorationCard": {
      "phase": "preguntar",
      "description": "Una sola pregunta progresiva. Nunca un cuestionario. El agente la emite una a la vez y la siguiente depende de la respuesta.",
      "props": {
        "pregunta": {
          "type": "string",
          "required": true
        },
        "porQuePregunto": {
          "type": "string",
          "required": true,
          "description": "Transparencia: por que esta pregunta importa para la recomendacion."
        },
        "tipo": {
          "type": "enum",
          "values": [
            "opcion",
            "escala",
            "monto",
            "texto"
          ],
          "required": true
        },
        "opciones": {
          "type": "array<{ id: string, label: string }>"
        },
        "permiteOtro": {
          "type": "boolean",
          "description": "Agrega una opcion 'Otro' con campo de texto libre. Uso principal: desambiguar cuando el agente no esta seguro de que pidio el usuario."
        },
        "slot": {
          "type": "string",
          "required": true,
          "description": "Slot de UserUnderstanding que esta pregunta busca llenar."
        },
        "action": {
          "type": "action",
          "required": true
        }
      }
    },
    "UnderstandingSummary": {
      "phase": "validar",
      "description": "Requisito del reto: el agente devuelve lo que entendio y el usuario confirma o corrige antes de avanzar.",
      "props": {
        "objetivoEntendido": {
          "type": "string",
          "required": true,
          "bindable": true
        },
        "supuestos": {
          "type": "array<string>",
          "required": true,
          "bindable": true
        },
        "confianza": {
          "type": "number",
          "bindable": true
        },
        "action": {
          "type": "action",
          "required": true
        }
      }
    },
    "DebtSimulator": {
      "phase": "resultado",
      "description": "Simulador de reestructura. Recibe en 'opciones' los plazos YA calculados por simulate_restructure y cambia entre ellos en el cliente, sin volver al agente. El calculo lo hace la tool MCP, nunca el LLM.",
      "props": {
        "saldo": {
          "type": "number",
          "required": true,
          "bindable": true
        },
        "opciones": {
          "type": "array<{ meses: number, cat: number, pagoMensual: number, costoTotal: number, interesesTotales: number }>",
          "required": true,
          "bindable": true,
          "description": "El arreglo completo que devuelve simulate_restructure. Manda SIEMPRE todos los plazos: cambiar de plazo es instantaneo porque los numeros ya viajaron."
        },
        "plazoSeleccionado": {
          "type": "number",
          "bindable": true
        },
        "action": {
          "type": "action",
          "required": true
        }
      }
    },
    "OptionComparator": {
      "phase": "resultado",
      "description": "Compara alternativas lado a lado con costo total y trade-offs explicitos. Obligatorio mostrar el costo, no solo el beneficio.",
      "props": {
        "titulo": {
          "type": "string",
          "required": true
        },
        "opciones": {
          "type": "array<{ id: string, nombre: string, pagoMensual: number, costoTotal: number, plazoMeses: number, ventaja: string, desventaja: string }>",
          "required": true,
          "bindable": true
        },
        "action": {
          "type": "action",
          "required": true
        }
      }
    },
    "ActionPlan": {
      "phase": "resultado",
      "description": "Ruta de accion. El paso final dispara una tool MCP de tipo write y exige confirmacion del usuario.",
      "props": {
        "titulo": {
          "type": "string",
          "required": true
        },
        "pasos": {
          "type": "array<{ titulo: string, detalle: string, requiereConfirmacion: boolean }>",
          "required": true,
          "bindable": true
        },
        "action": {
          "type": "action",
          "required": true
        }
      }
    },
    "CardShowcase": {
      "description": "Vitrina de tarjetas de credito. Muestra SOLO la imagen del plastico; al pasar el cursor aparecen los beneficios y el boton Ver mas. Usala cuando el usuario pregunte por tarjetas. Lista TODAS las que salgan elegibles en get_eligible_cards, no solo tres.",
      "props": {
        "titulo": {
          "type": "string",
          "required": true
        },
        "tarjetas": {
          "type": "array<{ id: string, nombre: string, imagen: string, bullets: string[], cat: number, anualidad: number, fuente: string, fechaVerificacion: string }>",
          "required": true,
          "bindable": true,
          "description": "imagen es la ruta que da get_card_catalog. cat y anualidad SIEMPRE de la tool."
        },
        "action": {
          "type": "action",
          "required": true
        },
        "destacadaId": {
          "type": "string",
          "bindable": true,
          "description": "id de la tarjeta que viene con recomendada:true en get_eligible_cards. La UI le pone marco rojo y un sello 'La que mas te conviene'. No la elijas tu: copia la que marco la tool."
        }
      }
    },
    "CardRanking": {
      "description": "Grafica de barras horizontales con las tarjetas mas recomendadas para este cliente, ordenadas de mayor a menor. Acompania siempre a CardShowcase. Tres a cinco barras: es el resumen del ranking, no el catalogo completo.",
      "props": {
        "titulo": {
          "type": "string",
          "required": true
        },
        "criterio": {
          "type": "string",
          "required": true,
          "description": "Que mide la barra, en palabras del usuario. Ej: 'Que tanto te conviene segun tu ingreso y tu uso de credito'."
        },
        "barras": {
          "type": "array<{ id: string, nombre: string, puntaje: number, porQue: string }>",
          "required": true,
          "bindable": true,
          "description": "puntaje de 0 a 100. porQue explica en una linea, con una cifra de la tool."
        },
        "action": {
          "type": "action"
        },
        "destacadaId": {
          "type": "string",
          "bindable": true,
          "description": "id de la tarjeta recomendada. Se marca con un sello junto a su barra."
        }
      }
    },
    "RiskAlert": {
      "phase": "analizar",
      "description": "Aviso de riesgo de endeudamiento. En nivel 'alto' se pinta en rojo y le dice al usuario que NO conviene sacar mas credito ahora. Copia nivel, titulo, mensaje y senales tal cual de alertaDeuda en get_eligible_cards: es una evaluacion del banco, no tu opinion. Cuando alertaDeuda.desaconsejaNuevoCredito sea true, esta surface va ARRIBA de todo.",
      "props": {
        "nivel": {
          "type": "enum",
          "values": [
            "ok",
            "precaucion",
            "alto"
          ],
          "required": true
        },
        "titulo": {
          "type": "string",
          "required": true,
          "bindable": true
        },
        "mensaje": {
          "type": "string",
          "required": true,
          "bindable": true
        },
        "senales": {
          "type": "array<string>",
          "bindable": true,
          "description": "Cifras concretas del perfil que sostienen la alerta. Copialas de la tool."
        }
      }
    },
    "OutOfScopeCard": {
      "phase": "preguntar",
      "description": "Respuesta a una consulta que Tu rumbo Banorte no puede atender: un saludo, una charla suelta o un tema ajeno a las finanzas del cliente. Dice que no se puede responder eso, explica que SI se puede hacer y ofrece preguntas listas para usar. No es un mensaje de error: es la puerta de entrada para quien no sabe que preguntar.",
      "props": {
        "titulo": {
          "type": "string",
          "required": true,
          "bindable": true
        },
        "mensaje": {
          "type": "string",
          "required": true,
          "bindable": true,
          "description": "Por que no se puede responder eso, en una o dos lineas y sin reganar."
        },
        "sugerencias": {
          "type": "array<{ id: string, texto: string, porQue: string }>",
          "required": true,
          "bindable": true,
          "description": "Preguntas que Tu rumbo Banorte SI resuelve. 'texto' se manda tal cual como consulta al picarla, asi que escribela como la escribiria el usuario. 'porQue' dice en una linea que obtiene si la pica."
        },
        "consultaOriginal": {
          "type": "string",
          "bindable": true,
          "description": "Lo que el usuario habia escrito. Si viene, la UI agrega un boton para mandarla al agente de todos modos: es la salida cuando el filtro se equivoco."
        },
        "action": {
          "type": "action",
          "required": true
        }
      }
    },
    "HeadlineVerdict": {
      "phase": "analizar",
      "description": "NIVEL 1 de la pantalla: la conclusion, en grande y sin rodeos. Un dato protagonista, una linea que lo interpreta y, si aplica, una barra que lo situa. Va SIEMPRE arriba de todo. No es un parrafo: si necesitas explicar mas, eso va en los widgets de abajo.",
      "props": {
        "veredicto": {
          "type": "string",
          "required": true,
          "bindable": true,
          "description": "La conclusion en UNA linea, maximo 90 caracteres. Es lo primero que lee el usuario."
        },
        "dato": {
          "type": "string",
          "required": true,
          "bindable": true,
          "description": "La cifra protagonista, ya formateada ($16,525 / 94% / 591). Copiada de una tool."
        },
        "datoEtiqueta": {
          "type": "string",
          "required": true,
          "description": "Que es esa cifra, en dos o tres palabras."
        },
        "tono": {
          "type": "enum",
          "values": [
            "neutral",
            "positive",
            "warning",
            "critical"
          ],
          "default": "neutral"
        },
        "indicador": {
          "type": "{ valor: number, etiqueta: string }",
          "bindable": true,
          "description": "Barra de 0 a 100 que situa el dato (uso de linea, score sobre 850, avance). Omitela si la cifra no es un porcentaje ni una posicion en un rango."
        },
        "apoyo": {
          "type": "array<{ label: string, value: string }>",
          "bindable": true,
          "description": "De UNO a TRES datos de contexto, cortos. Nunca mas de tres: esto es el titular, no el reporte."
        }
      }
    },
    "ProductPortfolio": {
      "phase": "analizar",
      "description": "NIVEL 2: los productos que el cliente YA tiene, como mosaico visual. Responde 'que productos tengo' de un vistazo. Los datos salen de get_my_products: contratados con su cifra, y los no contratados como etiquetas al pie. Nunca inventes el detalle de un credito: el seed no lo trae y la tool lo dice en su campo `limitacion`.",
      "props": {
        "titulo": {
          "type": "string",
          "required": true
        },
        "productos": {
          "type": "array<{ id: string, tipo: string, familia: string, valor: string, etiqueta: string, nota: string }>",
          "required": true,
          "bindable": true,
          "description": "Uno por producto CONTRATADO. `familia` es cuenta|tarjeta|credito|inversion y elige el icono. `valor` es la cifra ya formateada, o \"\" si la tool no trae ninguna. `nota` es opcional, una linea corta."
        },
        "sinContratar": {
          "type": "array<string>",
          "bindable": true,
          "description": "Nombres de los productos que NO tiene, para que sepa que existe. Van como etiquetas apagadas al pie, sin venderlos."
        },
        "action": {
          "type": "action"
        }
      }
    },
    "NextSteps": {
      "phase": "resultado",
      "description": "NIVEL 3: en que puede seguir el usuario. Dos a cuatro botones que al picarse mandan su texto como una consulta nueva, tal cual. NO es un flujo de contratacion ni un plan de pasos: son caminos para seguir explorando ('Comparar con otras tarjetas', 'Ver que pasa si pago mas al mes'). Cierra con esto en vez de con un parrafo.",
      "props": {
        "titulo": {
          "type": "string"
        },
        "pasos": {
          "type": "array<{ id: string, texto: string }>",
          "required": true,
          "bindable": true,
          "description": "`texto` se manda TAL CUAL como si el usuario lo hubiera escrito: redactalo en primera persona y que sea una consulta que si sepas resolver."
        },
        "action": {
          "type": "action",
          "required": true,
          "description": "Siempre { \"event\": { \"name\": \"siguiente_paso\" } }."
        }
      }
    },
    "BarChart": {
      "phase": "analizar",
      "description": "Grafica de barras verticales para COMPARAR cantidades de un vistazo: el CAT de varias tarjetas, la anualidad de cada una, la mensualidad por plazo. Usala en vez de escribir cifras comparadas en texto. Cada valor sale de una tool.",
      "props": {
        "titulo": {
          "type": "string",
          "required": true
        },
        "subtitulo": {
          "type": "string",
          "description": "Una linea: que se compara."
        },
        "unidad": {
          "type": "enum",
          "values": [
            "$",
            "%",
            ""
          ],
          "default": "$"
        },
        "barras": {
          "type": "array<{ id: string, label: string, valor: number, secundario: number, destacado: boolean, pie: string }>",
          "required": true,
          "bindable": true,
          "description": "De 2 a 8 barras. valor es NUMERO sin formato. secundario (opcional) se apila encima, p.ej. intereses sobre capital. destacado marca en rojo pleno la que importa. pie es una linea chica bajo la etiqueta."
        },
        "leyenda": {
          "type": "array<string>",
          "description": "Solo si usas secundario: [nombre de valor, nombre de secundario]."
        }
      }
    },
    "DonutChart": {
      "phase": "analizar",
      "description": "Dona para mostrar PARTES DE UN TODO: como se reparte el ingreso entre gasto y ahorro, que parte de la linea de credito esta usada contra la disponible. Solo si los segmentos de verdad suman un total que tenga sentido. Cada valor sale de una tool.",
      "props": {
        "titulo": {
          "type": "string",
          "required": true
        },
        "subtitulo": {
          "type": "string"
        },
        "unidad": {
          "type": "enum",
          "values": [
            "$",
            "%",
            ""
          ],
          "default": "$"
        },
        "segmentos": {
          "type": "array<{ id: string, label: string, valor: number }>",
          "required": true,
          "bindable": true,
          "description": "De 2 a 5 segmentos. valor es NUMERO sin formato."
        },
        "centro": {
          "type": "{ valor: string, etiqueta: string }",
          "description": "Texto en medio de la dona: el total ya formateado y que es."
        }
      }
    },
    "LineChart": {
      "phase": "analizar",
      "description": "Grafica de LINEA para evolucion en el tiempo: historial crediticio (score mes a mes), saldo de la tarjeta, ahorro acumulado. Hasta tres series. Muestra sola cuanto cambio de punta a punta. Los datos salen de get_financial_history.",
      "props": {
        "titulo": {
          "type": "string",
          "required": true
        },
        "subtitulo": {
          "type": "string"
        },
        "unidad": {
          "type": "enum",
          "values": [
            "$",
            "%",
            "pts",
            ""
          ],
          "default": ""
        },
        "etiquetas": {
          "type": "array<string>",
          "required": true,
          "bindable": true,
          "description": "Un mes por punto, en orden: [\"Ene\", ..., \"Ago\"]."
        },
        "series": {
          "type": "array<{ id: string, nombre: string, valores: number[] }>",
          "required": true,
          "bindable": true,
          "description": "Uno a tres. valores tiene UN numero por etiqueta, copiado de la tool."
        },
        "referencia": {
          "type": "{ valor: number, etiqueta: string }",
          "description": "Linea punteada de referencia, p.ej. { valor: 670, etiqueta: \"Score bueno\" }."
        }
      }
    },
    "ProgressBars": {
      "phase": "analizar",
      "description": "BARRAS DE PROGRESO para el finance score (total y componentes) o para los planes de ahorro contra su meta. Cada barra lleva su nota con la cifra que la explica. Todo sale de get_financial_history: financeScore y planesAhorro ya vienen calculados, no los estimes.",
      "props": {
        "titulo": {
          "type": "string",
          "required": true
        },
        "subtitulo": {
          "type": "string"
        },
        "total": {
          "type": "{ valor: number, maximo: number, etiqueta: string, nivel: string }",
          "description": "El numero grande de arriba: financeScore.total, maximo 100, y su nivel."
        },
        "barras": {
          "type": "array<{ id: string, label: string, valor: number, meta: number, unidad: string, nota: string, tono: string }>",
          "required": true,
          "bindable": true,
          "description": "Componentes del score (valor 0-100) o planes de ahorro (valor = acumulado, meta = meta, unidad \"$\")."
        }
      }
    }
  },
  "actions": {
    "description": "Todo evento emitido por un componente regresa al agente como contexto.",
    "shape": {
      "event": {
        "name": "string",
        "payload": "object"
      }
    }
  }
}
